description "start and stop the vexor worker"
version "1.0"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

console log

script
  home=/var/vexor/worker
  user=vx-worker
  command=/opt/vexor/bin/worker

  chdir $home
  su -s /bin/sh -c "exec $command" $user
end script

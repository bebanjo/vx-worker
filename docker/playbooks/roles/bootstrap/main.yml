---
- hosts: all

  tasks:
    - name: bootstrap | install packages
      apt: pkg=$item state=latest install_recommends=no
      with_items:
        - python-apt
        - python-pycurl
        - git-core
        - patch
        - curl
